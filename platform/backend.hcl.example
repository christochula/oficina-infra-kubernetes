# Copy to backend.hcl and replace placeholders. Never commit backend.hcl.
bucket       = "replace-with-terraform-state-bucket"
key          = "oficina/homolog/platform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true
